import 'package:flutter/material.dart';

class BurnDetectionScreen extends StatelessWidget {
  const BurnDetectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
